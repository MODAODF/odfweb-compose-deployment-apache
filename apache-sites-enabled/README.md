# apache-sites-enabled

Apache httpd Virtual Host configuration files drop-in directory.

This directory is mounted into the Apache httpd container at `/etc/apache2/sites-enabled/` and is used to customize virtual host configuration files.
